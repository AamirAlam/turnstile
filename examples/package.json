{
  "name": "turnstile-examples",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build needed for examples'",
    "start": "ts-node --project tsconfig.json src/server.ts",
    "demo:bot": "ts-node --project tsconfig.json src/bot.ts"
  },
  "dependencies": {
    "@turnstile/client": "workspace:*",
    "@turnstile/express": "workspace:*",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.25"
  }
}

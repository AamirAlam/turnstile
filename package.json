{
  "name": "turnstile-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples"
  ],
  "scripts": {
    "build": "pnpm run -r build",
    "start": "pnpm run --filter turnstile-examples start",
    "demo:bot": "pnpm run --filter turnstile-examples demo:bot"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.19"
  }
}
